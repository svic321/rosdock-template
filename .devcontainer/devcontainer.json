{
  "name": "ROS 2 Development Container",
  "privileged": true,
  "remoteUser": "pedrowsl",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "USERNAME": "pedrowsl"
    }
  },
  "workspaceFolder": "/home/ws",
  "workspaceMount": "source=${localWorkspaceFolder},target=/home/ws,type=bind",
  "initializeCommand": "bash .devcontainer/init-xauth.sh",
  "customizations": {
    "vscode": {
      "extensions":[
        "ms-vscode.cpptools",
        "ms-vscode.cpptools-themes",
        "twxs.cmake",
        "donjayamanne.python-extension-pack",
        "eamodio.gitlens",
        "ms-iot.vscode-ros"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  },
  "containerEnv": {
    "DISPLAY": "unix:0",
    "XAUTHORITY": "/tmp/.devcontainer.xauth",
    "ROS_AUTOMATIC_DISCOVERY_RANGE": "LOCALHOST",
    "ROS_DOMAIN_ID": "42",
    "SSH_AUTH_SOCK": "/ssh-agent",
    "TERM": "xterm-256color",
    "COLORTERM": "truecolor",
    "QT_SCALE_FACTOR": "1.5",
    "QT_AUTO_SCREEN_SCALE_FACTOR": "0"
  },
  "runArgs": [
    "--net=host",
    "--pid=host",
    "--ipc=host",
    "-e", "DISPLAY=${env:DISPLAY}"
  ],
  "mounts": [
    "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind,consistency=cached",
    // Xauthority cookie regenerated for the container's DISPLAY, see init-xauth.sh
    "source=${localEnv:HOME}/.devcontainer.xauth,target=/tmp/.devcontainer.xauth,type=bind,consistency=cached",
    // falls back to /dev/null when no SSH agent socket is available on the host
    "source=${localEnv:SSH_AUTH_SOCK:/dev/null},target=/ssh-agent,type=bind"
  ],
    "postCreateCommand": "bash .devcontainer/post-create.sh"
}
